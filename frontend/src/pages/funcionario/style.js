import styled from 'styled-components';

const Form = styled('form')`
    align-items: center;
    display: flex;
    justify-content: space-between;
    width: 100%;
    flex-wrap: wrap;
`;

export { Form };
